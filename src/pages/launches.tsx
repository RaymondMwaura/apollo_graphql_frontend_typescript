import React, { Fragment } from 'react';
import { RouteComponentProps } from '@reach/router';

type LaunchesProps = RouteComponentProps

const Launches: React.FC<LaunchesProps> = () => <div />;

export default Launches;
